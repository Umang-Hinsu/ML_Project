# Production Environment Variables for FastAPI Backend
PORT=8000
ENVIRONMENT=production

# Comma-separated list of allowed frontend URLs for CORS (or * for all)
ALLOWED_ORIGINS=https://your-frontend-domain.vercel.app,http://localhost:3000
